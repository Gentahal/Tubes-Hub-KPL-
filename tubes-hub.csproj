<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<OutputType>Exe</OutputType>
		<TargetFramework>net10.0</TargetFramework>
		<RootNamespace>tubes_hub</RootNamespace>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<ItemGroup>
		<Compile Remove="TubesHubGUI\**" />
		<Content Remove="TubesHubGUI\**" />
		<EmbeddedResource Remove="TubesHubGUI\**" />
		<None Remove="TubesHubGUI\**" />

		<Compile Remove="ModulProgressTests\**" />
		<Content Remove="ModulProgressTests\**" />
		<EmbeddedResource Remove="ModulProgressTests\**" />
		<None Remove="ModulProgressTests\**" />
	</ItemGroup>

	<ItemGroup>
		<None Update="team.json">
			<CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
		</None>
	</ItemGroup>

</Project>